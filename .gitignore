# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Secrets — never commit credentials
.env
.env.*
*.pem

# Editors / OS
.vscode/
.idea/
.DS_Store

# Test / build output
.output/
TestResults.xml
shortlist.json
shortlist.html
approvals.json
featured_media_before.json
curated_shortlist.json
fix_shortlist.json

# Restore journals from prune_low_value.py — these record live post IDs and
# are kept as workflow artifacts, not in the repo.
pruned-*.json
